import 'package:flutter/material.dart';

class AudioHomePage extends StatelessWidget {
  const AudioHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Audio Home Page"));
  }
}
